# Content — fill this in

Every word on the site comes from here. Fill in the blanks below, and it all gets typed up
into `content/site.ts` so the page components stay free of hardcoded text.

Anything left blank keeps its placeholder, so it's fine to fill this in gradually.

---

## The basics

| Field | Value |
|---|---|
| Her name (as shown in the greeting) | `_______` |
| Nickname / term of endearment | `_______` |
| Which birthday (e.g. 24th) | `_______` |
| Her actual birthday date | `_______` |
| Your name (the sender) | `_______` |

---

## The celebration

| Field | Value |
|---|---|
| What it is (dinner / surprise party / day trip / …) | `_______` |
| Date | `_______` |
| Start time | `_______` |
| Venue name | `_______` |
| Full address | `_______` |
| Google Maps link | `_______` |
| Dress code | `_______` |
| Anything she should bring | `_______` |
| Is it a surprise? | `_______` |

---

## Page copy

### `/` — Happy Birthday

- **Headline:** `HAPPY BIRTHDAY, _______`
- **Subline** (one warm sentence under the headline):
  > `_______`
- **Button text:** `_______` (default: *"Wait — there's more →"*)

### `/invitation` — You're invited

- **Opening line:** default *"You are cordially invited to"*
- **The occasion, in one line:**
  > `_______`
- **A short teaser** (2–3 sentences, no logistics — those come on the next page):
  > `_______`
- **Button text:** `_______` (default: *"When & where →"*)

### `/details`

- **"What to expect"** — 3 short bullets, no spoilers if it's a surprise:
  1. `_______`
  2. `_______`
  3. `_______`
- **Button text:** `_______` (default: *"I'll be there →"*)

### `/rsvp`

- **Question:** `_______` (default: *"So… will you come?"*)
- **Yes button:** `_______` (default: *"Yes ♥"*)
- **No button:** `_______` (default: *"No"* — this one runs away from the cursor)
- **After she says yes:**
  > `_______`

### `/letter`

The long message. Write as much as you want — this page is built for it.

> `_______`

---

## Photo captions (optional)

Only for photos you want a caption on. Key is the exact filename in `public/photos/`.

| Filename | Caption |
|---|---|
| `01-______.jpg` | `_______` |
| `02-______.jpg` | `_______` |

---

## Extras

| Field | Value |
|---|---|
| Background music track (file or link) | `_______` |
| Custom domain, if any | `_______` |
| Should RSVP notify you? (email / not needed) | `_______` |
